'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#C7815D',
    },
    secondary: {
      main: '#A8BDD7',
    },
  },
});

export default theme;
